apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: somatic-fusion-fastq-v2
  namespace: argo
spec:
  templates:
    # FFPE-tuned RNA-seq fusion workflow. Cloned from somatic-rna-fastq-v2
    # (somatic-testing-rna-gpu-fastq-v2) with these deltas vs. the parent
    # template:
    #   - trim-galore replaced with fastp (carries UMI extraction, default
    #     length 5)
    #   - dual STAR runs: existing star-fusion-template (STAR-Fusion-tuned)
    #     plus new star-align-arriba (Arriba-tuned chimeric params); each
    #     feeds its own caller
    #   - QC sidecar: samtools-sort-index → picard-markduplicates →
    #     picard-collect-rnaseq-metrics on the STAR-Fusion BAM
    #   - fusion-inspector after fusion-report for IGV-ready visualization
    #
    # Reference paths for ref-flat and ribosomal-intervals are resolved
    # inline from ref-base-dir for now. Promoting them to ref-paths is a
    # follow-up to load_and_prepare.yaml (see prototype plan).
    - name: somatic-testing-fusion-fastq
      inputs:
        parameters:
          - name: user-id
          - name: run-id
          - name: sample-id
          - name: sample-uuid
          - name: rna-input-files
          - name: tumor-type
          - name: ref-version
          - name: ref-paths
          - name: ref-base-dir
          - name: sample-input-dir
          - name: sample-output-dir
          - name: sample-temp-dir
          - name: sample-scratch-dir
          - name: thread-count
            default: "8"
          - name: umi-length
            default: "5"
      outputs:
        parameters:
          - name: bam-output
            valueFrom:
              parameter: "{{tasks.samtools-sort-index-sf.outputs.parameters.output-bam}}"
      dag:
        tasks:
          - name: validate-bed-file
            templateRef:
              name: bed-validator-template
              template: validate-bed-file
            arguments:
              parameters:
                - name: input-bed-file
                  value: "{{=jsonpath(inputs.parameters['rna-input-files'], '$.regionsFile')}}"
                - name: gtf-file
                  value: "{{inputs.parameters.ref-base-dir}}/{{inputs.parameters.ref-version}}/gene_annots/gencode.v49.basic.annotation.MANE_Select.gtf.gz"
                - name: chains-dir
                  value: "{{inputs.parameters.ref-base-dir}}/{{inputs.parameters.ref-version}}/other/chains/"
                - name: output-dir
                  value: "{{inputs.parameters.sample-temp-dir}}metrics/rna/bed_validation/"

          - name: compose-read-group-str
            templateRef:
              name: compose-read-group-str
              template: compose-read-group-str-from-fq
            arguments:
              parameters:
                - name: fq-r1
                  value: "{{=jsonpath(inputs.parameters['rna-input-files'], '$.fq1')}}"
                - name: sample-name
                  value: "{{inputs.parameters.sample-id}}"

          - name: pre-fastqc
            templateRef:
              name: fastqc-template
              template: fastqc
            arguments:
              parameters:
                - name: fq-r1
                  value: "{{=jsonpath(inputs.parameters['rna-input-files'], '$.fq1')}}"
                - name: fq-r2
                  value: "{{=jsonpath(inputs.parameters['rna-input-files'], '$.fq2')}}"
                - name: output-dir
                  value: "{{inputs.parameters.sample-temp-dir}}metrics/rna/fastqc/pre-trim/"

          - name: fastp
            templateRef:
              name: fastp-template
              template: fastp
            arguments:
              parameters:
                - name: sample-name
                  value: "{{inputs.parameters.sample-id}}"
                - name: fq-r1
                  value: "{{=jsonpath(inputs.parameters['rna-input-files'], '$.fq1')}}"
                - name: fq-r2
                  value: "{{=jsonpath(inputs.parameters['rna-input-files'], '$.fq2')}}"
                - name: thread-count
                  value: "{{inputs.parameters.thread-count}}"
                - name: umi-length
                  value: "{{inputs.parameters.umi-length}}"
                - name: output-dir
                  value: "{{inputs.parameters.sample-scratch-dir}}trimmed_reads/"
                - name: metrics-dir
                  value: "{{inputs.parameters.sample-temp-dir}}metrics/rna/fastp/"

          # rRNA quantification + depletion. Both downstream STARs ingest the
          # depleted FASTQs; the SortMeRNA log surfaces in MultiQC.
          # TODO(plan-followup): promote sortmerna-db-dir to a key in
          # ref-paths emitted by load_and_prepare-rna.
          - name: sortmerna
            templateRef:
              name: sortmerna-template
              template: sortmerna
            arguments:
              parameters:
                - name: sample-name
                  value: "{{inputs.parameters.sample-id}}"
                - name: fq-r1
                  value: "{{tasks.fastp.outputs.parameters.fq-r1}}"
                - name: fq-r2
                  value: "{{tasks.fastp.outputs.parameters.fq-r2}}"
                - name: sortmerna-db-dir
                  value: "{{inputs.parameters.ref-base-dir}}/{{inputs.parameters.ref-version}}/rna/sortmerna/"
                - name: output-dir
                  value: "{{inputs.parameters.sample-scratch-dir}}sortmerna/"
                - name: metrics-dir
                  value: "{{inputs.parameters.sample-temp-dir}}metrics/rna/sortmerna/"
                - name: thread-count
                  value: "{{inputs.parameters.thread-count}}"
            depends: "fastp"

          # STAR-Fusion-tuned alignment (bundled inside star-fusion-template).
          # Produces the BAM that feeds the QC sidecar (MarkDup → CollectRnaSeqMetrics → RustQC)
          # and downstream coverage tools (verifybamid, mosdepth).
          - name: star-fusion
            templateRef:
              name: star-fusion-template
              template: star-fusion
            arguments:
              parameters:
                - name: sample-id
                  value: "{{inputs.parameters.sample-id}}"
                - name: read-group-str
                  value: "{{tasks.compose-read-group-str.outputs.parameters.read-group-str-no-rg-prefix}}"
                - name: fq-r1
                  value: "{{tasks.sortmerna.outputs.parameters.depleted-r1}}"
                - name: fq-r2
                  value: "{{tasks.sortmerna.outputs.parameters.depleted-r2}}"
                - name: thread-count
                  value: "{{inputs.parameters.thread-count}}"
                - name: output-bam
                  value: "{{inputs.parameters.sample-scratch-dir}}bam/{{inputs.parameters.sample-id}}_unsorted_RNA.bam"
                - name: output-fusions-tsv
                  value: "{{inputs.parameters.sample-temp-dir}}results/fusions/star-fusion/{{inputs.parameters.sample-id}}_starfusion.tsv"
                - name: output-chimeric-out-fusions
                  value: "{{inputs.parameters.sample-temp-dir}}results/fusions/star-fusion/{{inputs.parameters.sample-id}}_Chimeric.out.junction"
                - name: metrics-dir
                  value: "{{inputs.parameters.sample-temp-dir}}metrics/rna/star-fusion/"
                - name: scratch-dir
                  value: "{{inputs.parameters.sample-scratch-dir}}star-fusion/"
                - name: ctat-genome-lib-build-dir
                  value: "{{=jsonpath(inputs.parameters['ref-paths'], '$.rna.ctat-genome-lib-build-dir')}}"
            depends: "sortmerna && compose-read-group-str"

          # Arriba-tuned STAR run. Same inputs as star-fusion but different
          # chimeric parameter set (WithinBAM HardClip, chimSegmentMin 10).
          # Output BAM is consumed only by Arriba.
          - name: star-align-arriba
            templateRef:
              name: star-align-template
              template: star-align-arriba
            arguments:
              parameters:
                - name: sample-id
                  value: "{{inputs.parameters.sample-id}}"
                - name: read-group-str
                  value: "{{tasks.compose-read-group-str.outputs.parameters.read-group-str-no-rg-prefix}}"
                - name: fq-r1
                  value: "{{tasks.sortmerna.outputs.parameters.depleted-r1}}"
                - name: fq-r2
                  value: "{{tasks.sortmerna.outputs.parameters.depleted-r2}}"
                - name: thread-count
                  value: "{{inputs.parameters.thread-count}}"
                - name: output-bam
                  value: "{{inputs.parameters.sample-scratch-dir}}bam/{{inputs.parameters.sample-id}}_arriba_unsorted.bam"
                - name: output-chimeric-out-fusions
                  value: "{{inputs.parameters.sample-scratch-dir}}bam/{{inputs.parameters.sample-id}}_Chimeric.out.junction"
                - name: metrics-dir
                  value: "{{inputs.parameters.sample-temp-dir}}metrics/rna/star-arriba/"
                - name: scratch-dir
                  value: "{{inputs.parameters.sample-scratch-dir}}star-arriba/"
                - name: ctat-genome-lib-build-dir
                  value: "{{=jsonpath(inputs.parameters['ref-paths'], '$.rna.ctat-genome-lib-build-dir')}}"
            depends: "sortmerna && compose-read-group-str"

          - name: samtools-sort-index-sf
            templateRef:
              name: samtools-template
              template: samtools-remove-orphans-sort-index
            arguments:
              parameters:
                - name: input-bam
                  value: "{{tasks.star-fusion.outputs.parameters.output-bam}}"
                - name: output-bam
                  value: "{{inputs.parameters.sample-temp-dir}}bam/{{inputs.parameters.sample-id}}_RNA.bam"
                - name: thread-count
                  value: "{{inputs.parameters.thread-count}}"
            depends: "star-fusion"

          - name: samtools-sort-index-arriba
            templateRef:
              name: samtools-template
              template: samtools-sort-index
            arguments:
              parameters:
                - name: input-bam
                  value: "{{tasks.star-align-arriba.outputs.parameters.output-bam}}"
                - name: output-bam
                  value: "{{inputs.parameters.sample-scratch-dir}}bam/{{inputs.parameters.sample-id}}_arriba.bam"
                - name: thread-count
                  value: "{{inputs.parameters.thread-count}}"
            depends: "star-align-arriba"

          - name: picard-markduplicates
            templateRef:
              name: picard-markduplicates-template
              template: picard-markduplicates
            arguments:
              parameters:
                - name: sample-id
                  value: "{{inputs.parameters.sample-id}}"
                - name: input-bam
                  value: "{{tasks.samtools-sort-index-sf.outputs.parameters.output-bam}}"
                - name: output-bam
                  value: "{{inputs.parameters.sample-temp-dir}}bam/{{inputs.parameters.sample-id}}_RNA.dedup.bam"
                - name: output-metrics
                  value: "{{inputs.parameters.sample-temp-dir}}metrics/rna/picard/{{inputs.parameters.sample-id}}_markdup_metrics.txt"
            depends: "samtools-sort-index-sf"

          - name: picard-collect-rnaseq-metrics
            templateRef:
              name: picard-collect-rnaseq-metrics-template
              template: picard-collect-rnaseq-metrics
            arguments:
              parameters:
                - name: sample-id
                  value: "{{inputs.parameters.sample-id}}"
                - name: input-bam
                  value: "{{tasks.picard-markduplicates.outputs.parameters.output-bam}}"
                # TODO(plan-followup): promote ref-flat and ribosomal-intervals
                # to keys in the ref-paths JSON emitted by load_and_prepare-rna.
                - name: ref-flat
                  value: "{{inputs.parameters.ref-base-dir}}/{{inputs.parameters.ref-version}}/gene_annots/refFlat_mane.txt"
                - name: ribosomal-intervals
                  value: "{{inputs.parameters.ref-base-dir}}/genome/GCA_000001405.15_GRCh38_full_minus_alt_plus_hs38d1_plus_HLA_analysis_set/rRNA.gencode.v46.interval_list"
                - name: output-metrics
                  value: "{{inputs.parameters.sample-temp-dir}}metrics/rna/picard/{{inputs.parameters.sample-id}}_rnaseq_metrics.txt"
            depends: "picard-markduplicates"

          - name: rustqc
            templateRef:
              name: rustqc-template
              template: rustqc
            arguments:
              parameters:
                - name: sample-id
                  value: "{{inputs.parameters.sample-id}}"
                - name: gene-annotation-gtf
                  value: "{{inputs.parameters.ref-base-dir}}/{{inputs.parameters.ref-version}}/gene_annots/gencode.v49.basic.annotation.MANE_Select.gtf.gz"
                - name: input-bam
                  value: "{{tasks.picard-markduplicates.outputs.parameters.output-bam}}"
                - name: output-dir
                  value: "{{inputs.parameters.sample-temp-dir}}metrics/rna/rustqc/"
                - name: thread-count
                  value: "{{inputs.parameters.thread-count}}"
            depends: "picard-markduplicates"

          - name: arriba
            templateRef:
              name: arriba-fusion-detection-template
              template: arriba-fusion-detection
            arguments:
              parameters:
                - name: sample-id
                  value: "{{inputs.parameters.sample-id}}"
                - name: input-bam
                  value: "{{tasks.samtools-sort-index-arriba.outputs.parameters.output-bam}}"
                - name: ref-genome-fa
                  value: "{{=jsonpath(inputs.parameters['ref-paths'], '$.genome')}}"
                - name: gene-annotation-gtf
                  value: "{{inputs.parameters.ref-base-dir}}/{{inputs.parameters.ref-version}}/gene_annots/gencode.v49.basic.annotation.MANE_Select.gtf.gz"
                - name: output-fusions-tsv
                  value: "{{inputs.parameters.sample-temp-dir}}results/fusions/arriba/{{inputs.parameters.sample-id}}_fusions.tsv"
                - name: output-fusions-discarded-tsv
                  value: "{{inputs.parameters.sample-temp-dir}}results/fusions/arriba/{{inputs.parameters.sample-id}}_fusions.discarded.tsv"
                - name: thread-count
                  value: "{{inputs.parameters.thread-count}}"
            depends: "samtools-sort-index-arriba"

          - name: fusion-report
            templateRef:
              name: fusion-report-template
              template: fusion-report
            arguments:
              parameters:
                - name: sample-id
                  value: "{{inputs.parameters.sample-id}}"
                - name: arriba-fusions-tsv
                  value: "{{tasks.arriba.outputs.parameters.output-fusions-tsv}}"
                - name: starfusion-predictions-tsv
                  value: "{{tasks.star-fusion.outputs.parameters.output-fusions-tsv}}"
                - name: fusion-report-db-dir
                  value: "{{inputs.parameters.ref-base-dir}}{{inputs.parameters.ref-version}}/other/fusion_report_db/"
                - name: output-dir
                  value: "{{inputs.parameters.sample-temp-dir}}results/fusions/fusion-report/"
                - name: metrics-dir
                  value: "{{inputs.parameters.sample-temp-dir}}metrics/rna/fusion-report/"
            depends: "arriba && star-fusion"

          # Pre-filter the Arriba-pass sorted BAM down to the read set FI
          # actually needs: split-reads (supplementary records) collected
          # around each surviving fusion breakpoint, with artifact gene
          # pairs dropped from the consolidated list. Feeding FI from the
          # upstream BAM (instead of raw fastp FASTQs) guarantees every
          # read that the FFPE-tuned upstream STAR aligned as a fusion
          # supporter is in the FI input pool — STAR-tuning inside FI
          # can't recover a read that was never handed to FI.
          # Arriba-pass BAM is the source because it carries chimerics
          # WithinBAM (HardClip); the STAR-Fusion-pass BAM emits chimerics
          # as a separate junction file and lacks the supplementary
          # records extract-reads keys on.
          - name: extract-fusion-reads
            templateRef:
              name: extract-reads-for-fusion-inspector-template
              template: extract-reads-for-fusion-inspector
            arguments:
              parameters:
                - name: sample-id
                  value: "{{inputs.parameters.sample-id}}"
                - name: input-bam
                  value: "{{tasks.samtools-sort-index-arriba.outputs.parameters.output-bam}}"
                - name: starfusion-tsv
                  value: "{{tasks.star-fusion.outputs.parameters.output-fusions-tsv}}"
                - name: arriba-tsv
                  value: "{{tasks.arriba.outputs.parameters.output-fusions-tsv}}"
                - name: fusion-list-tsv
                  value: "{{tasks.fusion-report.outputs.parameters.output-dir}}fusion_list.tsv"
                - name: output-dir
                  value: "{{inputs.parameters.sample-scratch-dir}}extracted_reads/"
                - name: thread-count
                  value: "{{inputs.parameters.thread-count}}"
            depends: "samtools-sort-index-arriba && fusion-report"

          # FusionInspector consumes fusion-report's consolidated gene-pair
          # list (geneA--geneB per line). Output dir holds IGV-ready BAM,
          # contig fasta, and pre-built session XML.
          - name: fusion-inspector
            templateRef:
              name: fusion-inspector-template
              template: fusion-inspector
            arguments:
              parameters:
                - name: sample-id
                  value: "{{inputs.parameters.sample-id}}"
                - name: fq-r1
                  value: "{{tasks.extract-fusion-reads.outputs.parameters.fq-r1}}"
                - name: fq-r2
                  value: "{{tasks.extract-fusion-reads.outputs.parameters.fq-r2}}"
                - name: fusion-list-tsv
                  value: "{{tasks.extract-fusion-reads.outputs.parameters.filtered-fusion-list-tsv}}"
                - name: ctat-genome-lib-build-dir
                  value: "{{=jsonpath(inputs.parameters['ref-paths'], '$.rna.ctat-genome-lib-build-dir')}}"
                - name: output-dir
                  value: "{{inputs.parameters.sample-temp-dir}}results/fusions/fusion-inspector/"
                - name: metrics-dir
                  value: "{{inputs.parameters.sample-temp-dir}}metrics/rna/fusion-inspector/"
                - name: scratch-dir
                  value: "{{inputs.parameters.sample-scratch-dir}}fusion-inspector/"
                - name: thread-count
                  value: "{{inputs.parameters.thread-count}}"
            depends: "extract-fusion-reads"

          - name: oncokb-fusion-annotation
            templateRef:
              name: oncokb-fusion-annotation-template
              template: oncokb-fusion-annotation
            arguments:
              parameters:
                - name: sample-id
                  value: "{{inputs.parameters.sample-id}}"
                - name: input-fusions-json
                  value: "{{tasks.fusion-report.outputs.parameters.output-fusions-json}}"
                - name: output-dir
                  value: "{{inputs.parameters.sample-temp-dir}}results/fusions/oncokb/"
                - name: tumor-type
                  value: "{{inputs.parameters.tumor-type}}"
            depends: "fusion-report"

          - name: duckdb-fusions
            templateRef:
              name: duckdb-template
              template: duckdb-fusions-v2
            arguments:
              parameters:
                - name: fusions-json-f
                  value: "{{tasks.oncokb-fusion-annotation.outputs.parameters.output-fusions-with-oncokb-json}}"
                - name: output-duckdb
                  value: "{{inputs.parameters.sample-temp-dir}}results/db/{{inputs.parameters.sample-id}}_fusions.duckdb"
            depends: "oncokb-fusion-annotation"

          - name: verifybamid
            templateRef:
              name: verifybamid-template
              template: verifybamid-exome-wrapper
            arguments:
              parameters:
                - name: input-bam
                  value: "{{tasks.samtools-sort-index-sf.outputs.parameters.output-bam}}"
                - name: output-dir
                  value: "{{inputs.parameters.sample-temp-dir}}metrics/rna/verifybamid/"
                - name: ref-version
                  value: "{{inputs.parameters.ref-version}}"
                - name: verifybamid-ref-base-dir
                  value: "{{inputs.parameters.ref-base-dir}}{{inputs.parameters.ref-version}}/other/verifybamid/"
            depends: "samtools-sort-index-sf"

          - name: mosdepth
            templateRef:
              name: mosdepth-template
              template: mosdepth
            arguments:
              parameters:
                - name: sample-id
                  value: "{{inputs.parameters.sample-id}}"
                - name: input-bam
                  value: "{{tasks.samtools-sort-index-sf.outputs.parameters.output-bam}}"
                - name: output-dir
                  value: "{{inputs.parameters.sample-temp-dir}}metrics/rna/mosdepth/"
                - name: regions-bed-f
                  value: "{{tasks.validate-bed-file.outputs.parameters.validated-bed-file}}"
            depends: "verifybamid && samtools-sort-index-sf && validate-bed-file"

          - name: multiqc
            templateRef:
              name: multiqc-template
              template: main
            arguments:
              parameters:
                - name: input-dir
                  value: "{{inputs.parameters.sample-temp-dir}}metrics/rna/"
                - name: output-dir
                  value: "{{inputs.parameters.sample-temp-dir}}metrics/rna/"
            depends: "verifybamid && samtools-sort-index-sf && mosdepth && picard-markduplicates && picard-collect-rnaseq-metrics && sortmerna && star-align-arriba && rustqc && fusion-report && (fusion-inspector.Succeeded || fusion-inspector.Skipped)"

          - name: collect-qc-data
            templateRef:
              name: collect-qc-data-v2-templates
              template: collect-qc-data-v2
            arguments:
              parameters:
                - name: sample-id
                  value: "{{inputs.parameters.sample-id}}"
                - name: run-id
                  value: "{{inputs.parameters.run-id}}"
                - name: sample-fqs-fnames
                  value: '["{{=sprig.osBase(jsonpath(inputs.parameters[''rna-input-files''], ''$.fq1''))}}", "{{=sprig.osBase(jsonpath(inputs.parameters[''rna-input-files''], ''$.fq2''))}}"]'
                - name: metrics-dir
                  value: "{{tasks.multiqc.outputs.parameters.output-dir}}"
                - name: data-source-type
                  value: "rna"
            depends: "multiqc && mosdepth && verifybamid"

          - name: copy-temp-to-output
            templateRef:
              name: copy-temp-to-output
              template: copy-temp-to-output-no-out
            arguments:
              parameters:
                - name: sample-id
                  value: "{{inputs.parameters.sample-id}}"
                - name: output-rel-dir
                  value: "{{inputs.parameters.sample-output-dir}}"
                - name: temp-rel-dir
                  value: "{{inputs.parameters.sample-temp-dir}}"
            depends: "collect-qc-data && duckdb-fusions && rustqc && fusion-inspector"

          - name: clean-scratch
            templateRef:
              name: dir-clean-up
              template: dir-clean-up
            arguments:
              parameters:
                - name: directory
                  value: "{{inputs.parameters.sample-scratch-dir}}"
            depends: "copy-temp-to-output"
